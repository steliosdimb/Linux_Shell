#include <vector>
#include <iostream>

using namespace std;

string push_to_history(vector<string>);
vector<string> is_myHistory(vector<char *>, vector<string>, int *, int, vector<string>, int *,vector<string>);
vector<string> check_range(vector<string>, vector<string>, int);