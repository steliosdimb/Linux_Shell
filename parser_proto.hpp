#include <vector>
#include <iostream>
using namespace std;
vector<string> remove_new_line(vector<string>);
vector<string> tokenize(void);
vector<char *> to_chararray(vector<string>);
vector<string> split_string_spaces(string);
void terminal_prints(void);
void signal_handler(int);
void signal_handler_p(int);